# shared package (planned)

Цель: убрать дублирование кода между `api-gateway`, `calc-bot`, `che-parser-bot`.

Рекомендуемый порядок миграции:
1. Вынести общий DB-клиент
2. Вынести общие утилиты (retry, logging helpers, env validation)
3. Вынести общие DTO/мапперы по мере стабилизации контрактов

Важно: переносить по одному модулю за итерацию с обратной совместимостью.
